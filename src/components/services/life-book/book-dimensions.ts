// Kich thuoc CO DINH cho 1 mat trang (Phase 2 shell) - dung chung giua
// Book.tsx (grid spread) va BookCover.tsx (bia rong = 2 lan PAGE_WIDTH) de 2
// noi khop kich thuoc, khong bi lech khi mo bia.
export const PAGE_WIDTH = 340;
export const PAGE_HEIGHT = 480;
